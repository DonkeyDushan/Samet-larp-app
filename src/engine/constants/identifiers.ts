/** Generated household IDs (`H_Marie`, `H_2_Marie_Mirek`); the prefix keeps them apart from character IDs. */
export const HOUSEHOLD_ID_PREFIX = 'H_'

/** Joins the parts of a generated household ID. */
export const HOUSEHOLD_ID_SEPARATOR = '_'

/** Joins the parts of internal lookup keys; `|` never appears in an author's ID. */
export const KEY_SEPARATOR = '|'

/** `S_Marie_Wealth_osobni=VALUE`: the number comes from the answer, not from the sheet (§6.7). */
export const VALUE_FROM_ANSWER = 'z_odpovedi'
