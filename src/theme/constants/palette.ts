/**
 * Primary actions stay neutral: colour is reserved for the run (§3.3) and for
 * severity, so a blue button never reads as "run A".
 */
export const LIGHT_PRIMARY_COLOR = '#262626'

export const DARK_PRIMARY_COLOR = '#e5e5e5'
