/** MUI defaults to 14 px; the org reads dense screens under time pressure (§6.4). */
export const BASE_FONT_SIZE_PX = 13

/** System faces only — no web font download, and Czech diacritics render everywhere. */
export const FONT_STACK = '"IBM Plex Sans", "Segoe UI", "Helvetica Neue", Arial, sans-serif'

/** Less rounded than Material's 4 px; reads plainer, closer to period print. */
export const BORDER_RADIUS_PX = 3

/** Tabs default to 48 px; the section bar should not outgrow the run bar above it. */
export const TAB_MIN_HEIGHT_PX = 36
